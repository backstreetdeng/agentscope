"""AgentScope 2.0.3 多智能体业务协作 Web 服务。

设计依据：
    - doc/乘用车市场战略决策AI智能体.html：系统首页、场景卡片、右侧 Agent 状态面板。
    - doc/市场战略决策智能体开发.html：输入执行区、智能体执行过程、分析结果区。
    - doc/中汽中心AI-UI设计规范.md：深蓝主色、灰白背景、8px 卡片、6px 按钮、4px 输入框。

运行方式：
    python app.py
"""

from __future__ import annotations

import asyncio
import json
import re
import subprocess
import sys
from datetime import datetime
from http import HTTPStatus
from http.server import BaseHTTPRequestHandler, ThreadingHTTPServer
from pathlib import Path
from threading import Lock
from typing import Any
from urllib.parse import parse_qs, unquote, urlparse

HOST = "127.0.0.1"
PORT = 7860
AGENT_RUN_LOCK = Lock()

# 候选文件当前放在 backup 目录中；未来也可能被复制到项目根目录并命名为 app.py。
# 这里先确定真实项目根目录，再导入 main/tools/utils，避免把 backup 误当成工程根。
APP_FILE = Path(__file__).resolve()
PROJECT_DIR = APP_FILE.parent.parent if APP_FILE.parent.name.lower() == "backup" else APP_FILE.parent
if str(PROJECT_DIR) not in sys.path:
    sys.path.insert(0, str(PROJECT_DIR))

from dotenv import load_dotenv
from openai import AuthenticationError
from agentscope.message import UserMsg

from main import (
    ENV_PATH,
    build_market_strategy_agent,
    build_user_insight_agent,
    expand_env,
    load_json_config,
    strip_thinking,
)
from tools.custom_tools import clear_tool_call_events, get_tool_call_events
from utils.audio_tagging_engine import DEFAULT_TAG_TAXONOMY, run_audio_tagging_workflow
from utils.html_ppt_builder import build_consulting_html_ppt
from utils.quality_gate import QUALITY_RULES, evaluate_report_quality


PPT_OUTPUT_DIR = PROJECT_DIR / "outputs" / "ppt"
HTML_REPORT_SKILL_RUNNER = (
    Path.home()
    / ".openclaw"
    / "workspace-report-agent"
    / "skills"
    / "html-report-generator"
    / "executors"
    / "run.py"
)
REPORT_FILE_TYPES = {
    ".txt": "文字报告",
    ".md": "Markdown报告",
    ".html": "HTML PPT",
}

HTML = '<!doctype html>\n<html lang="zh-CN">\n<head>\n  <meta charset="utf-8" />\n  <meta name="viewport" content="width=device-width, initial-scale=1" />\n  <title>乘用车市场战略决策AI智能体系统</title>\n  <style>\n    :root {\n      --primary: #003370;\n      --primary-light: #335599;\n      --bg-page: #F5F7FA;\n      --bg-card: #FFFFFF;\n      --text-title: #1A202C;\n      --text-body: #4A5568;\n      --text-muted: #94A3B8;\n      --border-divider: #E2E8F0;\n      --border-control: #CBD5E1;\n      --success: #059669;\n      --warning-bg: #FEF3C7;\n      --warning-text: #92400E;\n      --danger: #B91C1C;\n      --shadow-card: 0 1px 3px rgba(0,0,0,0.04);\n      --sidebar-width: 260px;\n      --agent-width: 280px;\n    }\n\n    * { box-sizing: border-box; }\n\n    body {\n      margin: 0;\n      min-height: 100vh;\n      background: var(--bg-page);\n      color: var(--text-body);\n      font-family: Inter, "PingFang SC", "Microsoft YaHei", -apple-system, BlinkMacSystemFont, sans-serif;\n      font-size: 15px;\n    }\n\n    button, textarea, select { font: inherit; }\n\n    .app-shell {\n      min-height: 100vh;\n      display: grid;\n      grid-template-columns: var(--sidebar-width) 1fr var(--agent-width);\n    }\n\n    .sidebar {\n      background: var(--bg-card);\n      border-right: 1px solid var(--border-divider);\n      padding: 22px 18px;\n      display: flex;\n      flex-direction: column;\n      gap: 20px;\n    }\n\n    .brand {\n      display: flex;\n      gap: 12px;\n      align-items: center;\n      padding-bottom: 18px;\n      border-bottom: 1px solid var(--border-divider);\n    }\n\n    .brand-mark {\n      width: 38px;\n      height: 38px;\n      border-radius: 8px;\n      background: var(--primary);\n      color: #fff;\n      display: grid;\n      place-items: center;\n      font-weight: 700;\n    }\n\n    .brand-title {\n      color: var(--text-title);\n      font-size: 15px;\n      font-weight: 600;\n      line-height: 1.35;\n    }\n\n    .brand-sub {\n      color: var(--text-muted);\n      font-size: 13px;\n      margin-top: 2px;\n    }\n\n    .nav-section-title {\n      color: var(--text-muted);\n      font-size: 13px;\n      margin: 4px 0 8px;\n    }\n\n    .nav-item {\n      width: 100%;\n      min-height: 42px;\n      padding: 11px 12px;\n      border: 0;\n      border-radius: 6px;\n      background: transparent;\n      display: flex;\n      align-items: center;\n      justify-content: space-between;\n      gap: 10px;\n      color: var(--text-body);\n      text-align: left;\n      cursor: pointer;\n    }\n\n    .nav-item.active, .nav-item:hover {\n      background: rgba(0, 51, 112, 0.08);\n      color: var(--primary);\n      font-weight: 500;\n    }\n\n    .workspace-view[hidden] { display: none; }\n\n    .db-card {\n      margin-top: auto;\n      background: var(--bg-card);\n      border: 1px solid var(--border-divider);\n      border-radius: 8px;\n      padding: 14px;\n    }\n\n    .db-card strong {\n      display: block;\n      color: var(--text-title);\n      font-size: 13px;\n      margin-bottom: 10px;\n    }\n\n    .db-line {\n      display: flex;\n      justify-content: space-between;\n      gap: 10px;\n      color: var(--text-muted);\n      font-size: 13px;\n      padding: 5px 0;\n      border-top: 1px solid #F1F5F9;\n    }\n\n    .workspace {\n      min-width: 0;\n      padding: 24px;\n      display: grid;\n      grid-template-rows: auto auto 1fr;\n      gap: 20px;\n    }\n\n    .topbar {\n      display: flex;\n      justify-content: space-between;\n      align-items: flex-start;\n      gap: 16px;\n    }\n\n    h1 {\n      margin: 0;\n      color: var(--text-title);\n      font-size: 28px;\n      line-height: 1.2;\n      font-weight: 600;\n      letter-spacing: 0;\n    }\n\n    .subtitle {\n      margin: 8px 0 0;\n      color: var(--text-body);\n      line-height: 1.7;\n    }\n\n    .status-pill {\n      height: 34px;\n      border: 1px solid var(--border-divider);\n      border-radius: 999px;\n      background: var(--bg-card);\n      display: inline-flex;\n      align-items: center;\n      gap: 8px;\n      padding: 0 12px;\n      white-space: nowrap;\n      color: var(--text-muted);\n      font-size: 13px;\n    }\n\n    .dot {\n      width: 8px;\n      height: 8px;\n      border-radius: 50%;\n      background: var(--border-control);\n    }\n\n    .dot.ok { background: var(--success); }\n    .dot.run { background: var(--primary); animation: pulse 1s infinite; }\n    .dot.bad { background: var(--danger); }\n\n    @keyframes pulse {\n      0%, 100% { transform: scale(1); opacity: .75; }\n      50% { transform: scale(1.35); opacity: 1; }\n    }\n\n    .scene-grid {\n      display: grid;\n      grid-template-columns: repeat(4, minmax(0, 1fr));\n      gap: 14px;\n    }\n\n    .scene-card {\n      background: var(--bg-card);\n      border: 1px solid var(--border-divider);\n      border-radius: 8px;\n      padding: 16px;\n      box-shadow: var(--shadow-card);\n      min-height: 116px;\n      cursor: pointer;\n    }\n\n    .scene-card.active, .scene-card:hover {\n      border-color: var(--primary);\n      box-shadow: 0 6px 18px rgba(0,51,112,0.10);\n    }\n\n    .scene-head {\n      display: flex;\n      align-items: center;\n      gap: 10px;\n      color: var(--text-title);\n      font-weight: 500;\n      font-size: 15px;\n    }\n\n    .scene-icon {\n      width: 32px;\n      height: 32px;\n      border-radius: 8px;\n      background: rgba(0, 51, 112, 0.08);\n      display: grid;\n      place-items: center;\n      color: var(--primary);\n      font-weight: 700;\n    }\n\n    .scene-desc {\n      margin-top: 10px;\n      color: var(--text-body);\n      font-size: 13px;\n      line-height: 1.55;\n    }\n\n    .work-grid {\n      min-height: 0;\n      display: grid;\n      grid-template-columns: minmax(360px, 0.92fr) minmax(460px, 1.08fr);\n      gap: 20px;\n    }\n\n    .panel {\n      min-width: 0;\n      background: var(--bg-card);\n      border: 1px solid var(--border-divider);\n      border-radius: 8px;\n      box-shadow: var(--shadow-card);\n      overflow: hidden;\n    }\n\n    .panel-head {\n      min-height: 58px;\n      padding: 16px 18px;\n      border-bottom: 1px solid var(--border-divider);\n      background: var(--bg-page);\n      display: flex;\n      align-items: center;\n      justify-content: space-between;\n      gap: 12px;\n    }\n\n    .panel-title {\n      color: var(--text-title);\n      font-size: 15px;\n      font-weight: 500;\n    }\n\n    .panel-body { padding: 18px; }\n\n    .agent-select {\n      display: grid;\n      grid-template-columns: 1fr 1fr;\n      gap: 10px;\n      margin-bottom: 14px;\n    }\n\n    .agent-tab {\n      border: 1px solid var(--border-divider);\n      background: var(--bg-card);\n      border-radius: 6px;\n      padding: 12px;\n      text-align: left;\n      color: var(--text-body);\n      cursor: pointer;\n    }\n\n    .agent-tab.active {\n      border-color: var(--primary);\n      color: var(--primary);\n      background: rgba(0, 51, 112, 0.04);\n    }\n\n    .agent-tab strong {\n      display: block;\n      font-size: 14px;\n      margin-bottom: 4px;\n    }\n\n    .agent-tab span {\n      display: block;\n      font-size: 12px;\n      color: var(--text-muted);\n      line-height: 1.45;\n    }\n\n    .quick-grid {\n      display: grid;\n      grid-template-columns: repeat(2, minmax(0, 1fr));\n      gap: 10px;\n      margin-bottom: 14px;\n    }\n\n    .quick {\n      border: 1px solid var(--border-divider);\n      background: var(--bg-card);\n      color: var(--text-body);\n      border-radius: 6px;\n      padding: 10px 12px;\n      text-align: left;\n      cursor: pointer;\n      min-height: 42px;\n    }\n\n    .quick:hover {\n      border-color: var(--primary);\n      color: var(--primary);\n    }\n\n    .skill-dock {\n      border: 1px solid var(--border-divider);\n      border-radius: 8px;\n      background: #FAFBFC;\n      padding: 12px;\n      margin-bottom: 14px;\n    }\n\n    .skill-dock-head {\n      display: flex;\n      justify-content: space-between;\n      align-items: center;\n      gap: 10px;\n      margin-bottom: 10px;\n    }\n\n    .skill-title {\n      color: var(--text-title);\n      font-size: 14px;\n      font-weight: 500;\n    }\n\n    .skill-hint {\n      color: var(--text-muted);\n      font-size: 12px;\n    }\n\n    .skill-grid {\n      display: grid;\n      grid-template-columns: repeat(3, minmax(0, 1fr));\n      gap: 8px;\n    }\n\n    .skill-chip {\n      min-height: 36px;\n      border: 1px solid var(--border-divider);\n      border-radius: 6px;\n      background: var(--bg-card);\n      color: var(--text-body);\n      cursor: pointer;\n      font-size: 13px;\n    }\n\n    .skill-chip.active, .skill-chip:hover {\n      border-color: var(--primary);\n      color: var(--primary);\n      background: rgba(0,51,112,0.04);\n    }\n\n    .flow-bar {\n      height: 8px;\n      border-radius: 999px;\n      background: #E2E8F0;\n      overflow: hidden;\n      margin: 12px 0;\n    }\n\n    .flow-fill {\n      width: 0%;\n      height: 100%;\n      background: linear-gradient(90deg, var(--primary), var(--primary-light));\n      transition: width 0.35s ease;\n    }\n\n    textarea {\n      width: 100%;\n      min-height: 150px;\n      border: 1px solid var(--border-control);\n      border-radius: 4px;\n      padding: 14px 16px;\n      font-size: 15px;\n      color: var(--text-title);\n      background: var(--bg-card);\n      resize: vertical;\n      outline: none;\n      line-height: 1.65;\n    }\n\n    textarea:focus, select:focus {\n      border-color: var(--primary);\n      box-shadow: 0 0 0 3px rgba(0,51,112,0.08);\n    }\n\n    .controls {\n      display: flex;\n      gap: 10px;\n      margin-top: 12px;\n    }\n\n    select {\n      flex: 1;\n      height: 42px;\n      border: 1px solid var(--border-control);\n      border-radius: 4px;\n      padding: 0 12px;\n      color: var(--text-body);\n      background: var(--bg-card);\n    }\n\n    .btn {\n      height: 42px;\n      padding: 0 16px;\n      border-radius: 6px;\n      border: 1px solid var(--primary);\n      background: var(--primary);\n      color: #fff;\n      cursor: pointer;\n      font-weight: 500;\n    }\n\n    .btn.secondary {\n      background: var(--bg-card);\n      color: var(--primary-light);\n      border-color: var(--primary-light);\n    }\n\n    .btn:disabled {\n      background: var(--border-control);\n      border-color: var(--border-control);\n      color: #fff;\n      cursor: wait;\n    }\n\n    .section-label {\n      margin: 18px 0 10px;\n      color: var(--text-title);\n      font-size: 15px;\n      font-weight: 500;\n      border-left: 3px solid var(--primary-light);\n      padding-left: 10px;\n    }\n\n    .steps {\n      display: grid;\n      gap: 10px;\n    }\n\n    .step {\n      border: 1px solid var(--border-divider);\n      border-radius: 8px;\n      background: var(--bg-card);\n      padding: 12px;\n    }\n\n    .step.active {\n      border-color: var(--primary);\n      background: rgba(0,51,112,0.04);\n    }\n\n    .step.done {\n      border-color: rgba(5,150,105,0.35);\n    }\n\n    .step-top {\n      display: flex;\n      justify-content: space-between;\n      gap: 12px;\n    }\n\n    .step-name {\n      color: var(--text-title);\n      font-size: 14px;\n      font-weight: 500;\n    }\n\n    .step-status, .step-summary {\n      color: var(--text-muted);\n      font-size: 12px;\n    }\n\n    .step-summary {\n      margin-top: 6px;\n      line-height: 1.5;\n    }\n\n    .log {\n      margin-top: 12px;\n      min-height: 84px;\n      max-height: 150px;\n      overflow: auto;\n      border: 1px solid var(--border-divider);\n      border-radius: 8px;\n      background: #0F172A;\n      color: #CBD5E1;\n      font: 12px/1.6 Consolas, "Cascadia Mono", monospace;\n      padding: 12px;\n      white-space: pre-wrap;\n    }\n\n    .metrics {\n      display: grid;\n      grid-template-columns: repeat(4, minmax(0, 1fr));\n      gap: 10px;\n      margin-bottom: 14px;\n    }\n\n    .metric {\n      border: 1px solid var(--border-divider);\n      border-radius: 8px;\n      padding: 12px;\n      background: var(--bg-card);\n    }\n\n    .metric-label {\n      color: var(--text-muted);\n      font-size: 12px;\n      margin-bottom: 6px;\n    }\n\n    .metric-value {\n      color: var(--primary);\n      font-size: 18px;\n      font-weight: 600;\n    }\n\n    .report {\n      min-height: 520px;\n      max-height: calc(100vh - 300px);\n      overflow: auto;\n      border: 1px solid var(--border-divider);\n      border-radius: 8px;\n      padding: 18px;\n      line-height: 1.75;\n      color: var(--text-body);\n      background: #fff;\n    }\n\n    .report h1, .report h2, .report h3 {\n      color: var(--text-title);\n      margin: 16px 0 10px;\n      line-height: 1.35;\n    }\n\n    .report h1 { font-size: 22px; }\n    .report h2 { font-size: 18px; }\n    .report h3 { font-size: 16px; }\n    .report p { margin: 8px 0; }\n    .report table { border-collapse: collapse; width: 100%; margin: 12px 0; font-size: 14px; }\n    .report th, .report td { border: 1px solid var(--border-divider); padding: 8px; text-align: left; vertical-align: top; }\n    .report th { background: var(--bg-page); color: var(--text-title); }\n\n    .report-cover {\n      border: 1px solid var(--border-divider);\n      border-left: 4px solid var(--primary);\n      border-radius: 8px;\n      background: #FAFBFC;\n      padding: 16px;\n      margin-bottom: 16px;\n    }\n\n    .report-cover-title {\n      color: var(--text-title);\n      font-size: 20px;\n      font-weight: 600;\n      margin-bottom: 8px;\n    }\n\n    .report-cover-meta {\n      display: grid;\n      grid-template-columns: repeat(4, minmax(0, 1fr));\n      gap: 8px;\n      color: var(--text-muted);\n      font-size: 12px;\n    }\n\n    .report-cover-meta span {\n      border: 1px solid var(--border-divider);\n      border-radius: 6px;\n      padding: 8px;\n      background: #fff;\n    }\n\n    .empty {\n      height: 360px;\n      display: grid;\n      place-items: center;\n      text-align: center;\n      color: var(--text-muted);\n    }\n\n    .empty strong {\n      color: var(--text-title);\n      font-size: 18px;\n    }\n\n    .report-library-panel {\n      min-height: calc(100vh - 146px);\n    }\n\n    .panel-subtitle {\n      color: var(--text-muted);\n      font-size: 13px;\n      margin-top: 4px;\n      line-height: 1.45;\n    }\n\n    .report-searchbar {\n      display: grid;\n      grid-template-columns: minmax(0, 1fr) auto;\n      gap: 10px;\n      margin-bottom: 12px;\n    }\n\n    .report-searchbar input {\n      width: 100%;\n      min-height: 40px;\n      border: 1px solid var(--border-control);\n      border-radius: 6px;\n      padding: 0 12px;\n      color: var(--text-title);\n      background: #fff;\n      outline: none;\n    }\n\n    .report-searchbar input:focus {\n      border-color: var(--primary);\n      box-shadow: 0 0 0 3px rgba(0,51,112,0.08);\n    }\n\n    .report-library-meta {\n      min-height: 24px;\n      color: var(--text-muted);\n      font-size: 13px;\n      margin-bottom: 12px;\n    }\n\n    .report-list {\n      display: grid;\n      gap: 12px;\n    }\n\n    .report-item {\n      border: 1px solid var(--border-divider);\n      border-radius: 8px;\n      padding: 14px;\n      background: #fff;\n      box-shadow: var(--shadow-card);\n    }\n\n    .report-item:hover {\n      border-color: rgba(0,51,112,0.35);\n      box-shadow: 0 8px 22px rgba(15,23,42,0.06);\n    }\n\n    .report-item-head {\n      display: flex;\n      justify-content: space-between;\n      align-items: flex-start;\n      gap: 12px;\n      margin-bottom: 8px;\n    }\n\n    .report-name {\n      color: var(--text-title);\n      font-size: 15px;\n      font-weight: 600;\n      line-height: 1.45;\n      word-break: break-word;\n    }\n\n    .report-type {\n      flex: 0 0 auto;\n      border: 1px solid rgba(0,51,112,0.16);\n      border-radius: 999px;\n      padding: 3px 9px;\n      color: var(--primary);\n      background: rgba(0,51,112,0.06);\n      font-size: 12px;\n      white-space: nowrap;\n    }\n\n    .report-snippet {\n      color: var(--text-body);\n      font-size: 13px;\n      line-height: 1.7;\n      margin: 8px 0 10px;\n    }\n\n    .report-file-meta {\n      color: var(--text-muted);\n      font-size: 12px;\n      display: flex;\n      flex-wrap: wrap;\n      gap: 10px;\n      margin-bottom: 12px;\n    }\n\n    .report-actions {\n      display: flex;\n      flex-wrap: wrap;\n      gap: 8px;\n    }\n\n    .link-btn {\n      min-height: 34px;\n      border: 1px solid var(--border-divider);\n      border-radius: 6px;\n      padding: 7px 11px;\n      color: var(--primary);\n      background: #fff;\n      font-size: 13px;\n      text-decoration: none;\n    }\n\n    .link-btn.primary {\n      border-color: var(--primary);\n      background: var(--primary);\n      color: #fff;\n    }\n\n    .tag-workbench {\n      display: grid;\n      gap: 16px;\n    }\n\n    .tag-command-grid {\n      display: grid;\n      grid-template-columns: minmax(0, 1fr) auto;\n      gap: 12px;\n      align-items: stretch;\n    }\n\n    .tag-command-grid textarea {\n      min-height: 96px;\n      margin: 0;\n    }\n\n    .tag-command-actions {\n      display: grid;\n      gap: 8px;\n      align-content: start;\n      min-width: 144px;\n    }\n\n    .tag-quick-area {\n      margin-top: 14px;\n      padding: 12px;\n      border: 1px solid var(--border-divider);\n      border-radius: 8px;\n      background: #FAFBFC;\n    }\n\n    .tag-quick-head {\n      display: flex;\n      align-items: center;\n      justify-content: space-between;\n      gap: 12px;\n      margin-bottom: 10px;\n      color: var(--text-title);\n      font-size: 14px;\n      font-weight: 500;\n    }\n\n    .tag-chip-row {\n      display: flex;\n      flex-wrap: wrap;\n      gap: 8px;\n    }\n\n    .tag-token {\n      min-height: 30px;\n      border: 1px solid var(--border-divider);\n      border-radius: 999px;\n      padding: 5px 10px;\n      background: #fff;\n      color: var(--text-body);\n      font-size: 12px;\n      cursor: pointer;\n    }\n\n    .tag-token.active, .tag-token:hover {\n      border-color: var(--primary);\n      background: rgba(0,51,112,0.06);\n      color: var(--primary);\n    }\n\n    .workflow-chain {\n      display: grid;\n      grid-template-columns: repeat(9, minmax(92px, 1fr));\n      gap: 8px;\n      overflow-x: auto;\n      padding-bottom: 2px;\n    }\n\n    .workflow-step-card {\n      min-height: 116px;\n      border: 1px solid var(--border-divider);\n      border-radius: 8px;\n      padding: 10px;\n      background: #fff;\n      position: relative;\n    }\n\n    .workflow-step-card.active {\n      border-color: var(--primary);\n      box-shadow: 0 6px 18px rgba(0,51,112,0.10);\n    }\n\n    .workflow-step-index {\n      color: var(--primary);\n      font-size: 12px;\n      font-weight: 700;\n      margin-bottom: 8px;\n    }\n\n    .workflow-step-name {\n      color: var(--text-title);\n      font-size: 13px;\n      font-weight: 600;\n      margin-bottom: 6px;\n    }\n\n    .workflow-step-output {\n      color: var(--text-muted);\n      font-size: 12px;\n      line-height: 1.45;\n    }\n\n    .tag-dashboard-grid {\n      display: grid;\n      grid-template-columns: minmax(360px, 0.9fr) minmax(460px, 1.1fr);\n      gap: 16px;\n    }\n\n    .tag-subgrid {\n      display: grid;\n      gap: 12px;\n    }\n\n    .taxonomy-box, .tag-record-card, .tag-insight-card, .dataset-row {\n      border: 1px solid var(--border-divider);\n      border-radius: 8px;\n      background: #fff;\n      padding: 12px;\n    }\n\n    .taxonomy-title {\n      color: var(--text-title);\n      font-size: 13px;\n      font-weight: 600;\n      margin-bottom: 8px;\n    }\n\n    .tag-record-card {\n      display: grid;\n      gap: 8px;\n    }\n\n    .tag-record-meta {\n      display: flex;\n      flex-wrap: wrap;\n      gap: 8px;\n      color: var(--text-muted);\n      font-size: 12px;\n    }\n\n    .tag-record-text {\n      color: var(--text-body);\n      font-size: 13px;\n      line-height: 1.65;\n    }\n\n    .tag-score-line {\n      display: grid;\n      grid-template-columns: 80px 1fr 42px;\n      gap: 8px;\n      align-items: center;\n      color: var(--text-muted);\n      font-size: 12px;\n    }\n\n    .mini-bar {\n      height: 8px;\n      border-radius: 999px;\n      background: #E2E8F0;\n      overflow: hidden;\n    }\n\n    .mini-bar span {\n      display: block;\n      height: 100%;\n      border-radius: inherit;\n      background: linear-gradient(90deg, var(--primary), var(--primary-light));\n    }\n\n    .stats-list {\n      display: grid;\n      gap: 9px;\n    }\n\n    .stat-row {\n      display: grid;\n      grid-template-columns: 92px 1fr 48px;\n      gap: 10px;\n      align-items: center;\n      color: var(--text-body);\n      font-size: 13px;\n    }\n\n    .stat-bar {\n      height: 10px;\n      border-radius: 999px;\n      background: #E2E8F0;\n      overflow: hidden;\n    }\n\n    .stat-bar span {\n      display: block;\n      height: 100%;\n      border-radius: inherit;\n      background: var(--primary);\n    }\n\n    .tag-empty-note {\n      color: var(--text-muted);\n      font-size: 13px;\n      line-height: 1.65;\n      padding: 12px;\n      border: 1px dashed var(--border-divider);\n      border-radius: 8px;\n      background: #FAFBFC;\n    }\n\n    .quality-panel {\n      border: 1px solid var(--border-divider);\n      border-radius: 8px;\n      background: #fff;\n      margin-bottom: 14px;\n      overflow: hidden;\n    }\n\n    .quality-summary {\n      display: flex;\n      justify-content: space-between;\n      align-items: center;\n      gap: 12px;\n      padding: 12px 14px;\n      background: #FAFBFC;\n      border-bottom: 1px solid var(--border-divider);\n    }\n\n    .quality-status { font-weight: 700; }\n    .quality-status.通过 { color: var(--success); }\n    .quality-status.待核验 { color: #B45309; }\n    .quality-status.风险, .quality-status.阻断 { color: var(--danger); }\n\n    .quality-grid {\n      display: grid;\n      grid-template-columns: repeat(2, minmax(0, 1fr));\n      gap: 10px;\n      padding: 12px;\n    }\n\n    .quality-item {\n      border: 1px solid var(--border-divider);\n      border-radius: 8px;\n      padding: 10px;\n      background: #fff;\n    }\n\n    .quality-item-head {\n      display: flex;\n      justify-content: space-between;\n      gap: 8px;\n    }\n\n    .quality-name {\n      color: var(--text-title);\n      font-weight: 600;\n      font-size: 13px;\n    }\n\n    .quality-badge {\n      flex: 0 0 auto;\n      border-radius: 999px;\n      padding: 2px 8px;\n      background: #F1F5F9;\n      font-size: 12px;\n    }\n\n    .quality-badge.通过 { color: var(--success); }\n    .quality-badge.待核验 { color: #B45309; }\n    .quality-badge.风险, .quality-badge.阻断 { color: var(--danger); }\n\n    .quality-detail {\n      color: var(--text-muted);\n      font-size: 12px;\n      margin-top: 6px;\n      line-height: 1.55;\n    }\n\n    .quality-actions {\n      padding: 0 12px 12px;\n      color: var(--text-body);\n      font-size: 13px;\n      line-height: 1.6;\n    }\n\n    .agent-panel {\n      background: var(--bg-card);\n      border-left: 1px solid var(--border-divider);\n      padding: 20px;\n      display: flex;\n      flex-direction: column;\n      gap: 18px;\n    }\n\n    .side-title {\n      color: var(--primary);\n      font-size: 14px;\n      font-weight: 600;\n    }\n\n    .agent-card {\n      border: 1px solid var(--border-divider);\n      border-radius: 8px;\n      background: #FAFBFC;\n      padding: 14px;\n    }\n\n    .agent-card.active {\n      border-color: var(--primary);\n      background: rgba(0, 51, 112, 0.04);\n    }\n\n    .agent-header {\n      display: flex;\n      align-items: center;\n      gap: 10px;\n      margin-bottom: 10px;\n    }\n\n    .agent-avatar {\n      width: 34px;\n      height: 34px;\n      border-radius: 8px;\n      background: var(--primary);\n      color: #fff;\n      display: grid;\n      place-items: center;\n      font-weight: 700;\n    }\n\n    .agent-name {\n      color: var(--text-title);\n      font-size: 14px;\n      font-weight: 600;\n    }\n\n    .agent-role {\n      color: var(--text-muted);\n      font-size: 12px;\n      margin-top: 2px;\n    }\n\n    .agent-status {\n      display: flex;\n      justify-content: space-between;\n      gap: 8px;\n      color: var(--text-muted);\n      font-size: 13px;\n      padding-top: 8px;\n      border-top: 1px solid var(--border-divider);\n    }\n\n    .action-btn {\n      width: 100%;\n      min-height: 40px;\n      border-radius: 6px;\n      border: 1px solid var(--primary);\n      background: var(--primary);\n      color: #fff;\n      cursor: pointer;\n      margin-top: 10px;\n    }\n\n    .action-btn.secondary {\n      background: #fff;\n      color: var(--primary-light);\n      border-color: var(--primary-light);\n    }\n\n    .notice {\n      background: linear-gradient(135deg, #FEF3C7 0%, #FDE68A 100%);\n      border-radius: 8px;\n      padding: 14px;\n      color: var(--warning-text);\n      font-size: 13px;\n      line-height: 1.65;\n    }\n\n    .mcp-grid {\n      display: grid;\n      gap: 8px;\n    }\n\n    .mcp-item {\n      border: 1px solid var(--border-divider);\n      border-radius: 8px;\n      background: #FAFBFC;\n      padding: 10px;\n    }\n\n    .mcp-item strong {\n      display: block;\n      color: var(--text-title);\n      font-size: 13px;\n      margin-bottom: 4px;\n    }\n\n    .mcp-item span {\n      display: block;\n      color: var(--text-muted);\n      font-size: 12px;\n      line-height: 1.45;\n    }\n\n    @media (max-width: 1180px) {\n      .app-shell {\n        grid-template-columns: var(--sidebar-width) 1fr;\n      }\n      .agent-panel { display: none; }\n      .scene-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }\n      .work-grid { grid-template-columns: 1fr; }\n      .tag-dashboard-grid { grid-template-columns: 1fr; }\n      .workflow-chain { grid-template-columns: repeat(9, minmax(120px, 1fr)); }\n    }\n\n    @media (max-width: 760px) {\n      .app-shell { grid-template-columns: 1fr; }\n      .sidebar { display: none; }\n      .workspace { padding: 16px; }\n      .topbar { display: block; }\n      .scene-grid, .quick-grid, .agent-select, .metrics, .skill-grid, .report-cover-meta, .report-searchbar { grid-template-columns: 1fr; }\n      .tag-command-grid { grid-template-columns: 1fr; }\n      .controls { flex-direction: column; }\n    }\n  </style>\n</head>\n<body>\n  <div class="app-shell">\n    <aside class="sidebar">\n      <div class="brand">\n        <div class="brand-mark">AI</div>\n        <div>\n          <div class="brand-title">乘用车市场战略决策AI智能体系统</div>\n          <div class="brand-sub">Strategy Orchestrator Live</div>\n        </div>\n      </div>\n\n      <div>\n        <div class="nav-section-title">主菜单</div>\n        <button class="nav-item active" data-view="analysis" type="button"><span>市场分析窗口</span><span>在线</span></button>\n        <button class="nav-item" data-view="analysis" data-agent-action="user_insight_config" type="button"><span>用户洞察</span><span>在线</span></button>\n        <button class="nav-item" data-view="tagging" type="button"><span>用户标签</span><span>音频调研</span></button>\n        <button class="nav-item" data-view="analysis" type="button"><span>配置管理</span><span>2 Agent</span></button>\n        <button class="nav-item" data-view="reports" type="button"><span>报告查询</span><span>资产库</span></button>\n      </div>\n\n      <div class="db-card">\n        <strong>当前模型连接</strong>\n        <div class="db-line"><span>Provider</span><span id="provider">检测中</span></div>\n        <div class="db-line"><span>Model</span><span id="model">-</span></div>\n        <div class="db-line"><span>Endpoint</span><span id="endpoint">-</span></div>\n      </div>\n    </aside>\n\n    <main class="workspace">\n      <div class="topbar">\n        <div>\n          <h1>战略决策分析场景</h1>\n          <p class="subtitle">选择业务智能体与分析场景，系统将按业务专家框架完成市场研判、用户洞察和配置定义；数据质量门禁会在报告返回后自动执行。</p>\n        </div>\n        <div class="status-pill"><span class="dot" id="statusDot"></span><span id="statusText">检查服务中</span></div>\n      </div>\n\n      <div class="workspace-view" id="analysisView">\n      <section class="agent-select top-agent-select">\n        <button class="agent-tab active" data-agent="market_strategy" type="button">\n          <strong>市场战略Agent</strong>\n          <span>机会识别、风险预警、产能调配、进入决策</span>\n        </button>\n        <button class="agent-tab" data-agent="user_insight_config" type="button">\n          <strong>用户洞察Agent</strong>\n          <span>用户圈层、需求配置匹配、选装优先级</span>\n        </button>\n      </section>\n\n      <section class="scene-grid">\n        <div class="scene-card active" data-agent="market_strategy" data-q="请输出业务报告：针对2026年15-20万元新能源SUV乘用车市场，识别2个高潜力增量机会市场和2个高风险衰退预警市场，输出市场分级、区域产能调配建议、进入/暂缓决策意见。每章最多3条要点，必须覆盖全部章节。">\n          <div class="scene-head"><span class="scene-icon">1</span><span>市场机会分析</span></div>\n          <div class="scene-desc">识别市场切入机会、增长潜力、竞争格局与空白市场。</div>\n        </div>\n        <div class="scene-card" data-agent="market_strategy" data-q="请分析2026年15-20万元新能源SUV的高风险衰退预警市场，并输出风险信号、触发条件和暂缓进入建议。">\n          <div class="scene-head"><span class="scene-icon">2</span><span>风险预警监控</span></div>\n          <div class="scene-desc">识别衰退信号、政策风险、区域库存与投放风险。</div>\n        </div>\n        <div class="scene-card" data-agent="user_insight_config" data-q="请针对15-20万元新能源SUV，锁定3类目标用户圈层，完成需求-配置匹配测算，并给出不超过8项整车选装配置优先级排序。">\n          <div class="scene-head"><span class="scene-icon">3</span><span>用户画像洞察</span></div>\n          <div class="scene-desc">生成目标用户画像，分析购车决策因子与核心需求。</div>\n        </div>\n        <div class="scene-card" data-agent="user_insight_config" data-q="请针对15-20万元新能源SUV，输出配置优先级排序、推荐配置包方案和成本/供应约束提示。">\n          <div class="scene-head"><span class="scene-icon">4</span><span>配置优先级排序</span></div>\n          <div class="scene-desc">基于用户需求和市场数据，推荐可执行配置方案。</div>\n        </div>\n      </section>\n\n      <section class="work-grid">\n        <div class="panel">\n          <div class="panel-head">\n            <div class="panel-title">输入与执行</div>\n            <button class="btn secondary" id="healthBtn" type="button">刷新状态</button>\n          </div>\n          <div class="panel-body">\n            <div class="agent-select">\n              <button class="agent-tab active" data-agent="market_strategy" type="button">\n                <strong>市场战略Agent</strong>\n                <span>机会识别、风险预警、产能调配、进入决策</span>\n              </button>\n              <button class="agent-tab" data-agent="user_insight_config" type="button">\n                <strong>用户洞察Agent</strong>\n                <span>用户圈层、需求配置匹配、选装优先级</span>\n              </button>\n            </div>\n\n            <div class="quick-grid" id="quickGrid"></div>\n\n            <div class="skill-dock">\n              <div class="skill-dock-head">\n                <div class="skill-title">Skill / 工具链能力</div>\n                <div class="skill-hint">点击后自动填充任务问题</div>\n              </div>\n              <div class="skill-grid">\n                <button class="skill-chip" data-skill="any_search" type="button">AnySearch搜索</button>\n                <button class="skill-chip" data-skill="web_fetch" type="button">web-fetch</button>\n                <button class="skill-chip" data-skill="tavily_search" type="button">Tavily搜索</button>\n                <button class="skill-chip" data-skill="playwright_scraper" type="button">动态网页抓取</button>\n                <button class="skill-chip" data-skill="agent_reach_search" type="button">agent-reach</button>\n                <button class="skill-chip" data-skill="multi_agent" type="button">双Agent联动</button>\n              </div>\n            </div>\n\n            <textarea id="questionInput"></textarea>\n\n            <div class="controls">\n              <select id="analysisType">\n                <option value="">自动识别类型</option>\n                <option value="opportunity">机会评估</option>\n                <option value="risk">风险预警</option>\n                <option value="user">用户洞察</option>\n                <option value="config">配置定义</option>\n              </select>\n              <button class="btn" id="runBtn" type="button">开始分析</button>\n            </div>\n\n            <div class="section-label">智能体执行过程</div>\n            <div class="flow-bar"><div class="flow-fill" id="flowFill"></div></div>\n            <div class="steps" id="steps"></div>\n            <div class="log" id="eventLog">等待执行...</div>\n          </div>\n        </div>\n\n        <div class="panel">\n          <div class="panel-head">\n            <div class="panel-title">分析结果</div>\n            <div>\n              <button class="btn secondary" id="copyBtn" type="button">复制报告</button>\n              <button class="btn secondary" id="leaderReportBtn" type="button">生成PPT版</button>\n              <button class="btn secondary" id="downloadMdBtn" type="button">下载MD</button>\n            </div>\n          </div>\n          <div class="panel-body">\n            <div class="metrics">\n              <div class="metric"><div class="metric-label">当前Agent</div><div class="metric-value" id="mAgent">市场</div></div>\n              <div class="metric"><div class="metric-label">质量门禁</div><div class="metric-value" id="mQuality">-</div></div>\n              <div class="metric"><div class="metric-label">工具状态</div><div class="metric-value" id="mTools">6</div></div>\n              <div class="metric"><div class="metric-label">输出长度</div><div class="metric-value" id="mLength">-</div></div>\n            </div>\n            <div class="quality-panel" id="qualityGatePanel">\n              <div class="quality-summary">\n                <div>\n                  <strong>数据质量门禁</strong>\n                  <div class="panel-subtitle">报告返回后自动审查；状态会影响 PPT 生成提醒。</div>\n                </div>\n                <span class="quality-status">待报告生成</span>\n              </div>\n            </div>\n            <div class="report" id="reportArea">\n              <div class="empty">\n                <div>\n                  <strong>还没有分析结果</strong>\n                  <p>选择智能体并点击“开始分析”后，这里会展示完整报告。</p>\n                </div>\n              </div>\n            </div>\n          </div>\n        </div>\n      </section>\n      </div>\n\n      <section class="workspace-view tag-workbench" id="tagWorkbenchView" hidden>\n        <div class="panel">\n          <div class="panel-head">\n            <div>\n              <div class="panel-title">用户标签与调研音频任务编排</div>\n              <div class="panel-subtitle">从原始访谈音频到 ASR、脱敏、切片、标签、评分、配置映射和可检索入库的端到端工作流。</div>\n            </div>\n            <button class="btn secondary" id="tagResetBtn" type="button">清空筛选</button>\n          </div>\n          <div class="panel-body">\n            <div class="tag-command-grid">\n              <textarea id="audioWorkflowInput">请编排访谈类音频处理任务：完成ASR转写、说话人分离、隐私脱敏、片段切分、用户标签、情绪/强度评分、配置映射，并沉淀为可检索向量库和结构化数据库。</textarea>\n              <div class="tag-command-actions">\n                <button class="btn" id="runAudioWorkflowBtn" type="button">执行编排</button>\n                <button class="btn secondary" id="exportTagCsvBtn" type="button">导出CSV</button>\n              </div>\n            </div>\n            <div class="tag-quick-area">\n              <div class="tag-quick-head">\n                <span>标签快速选择</span>\n                <span id="tagSelectionMeta">未选择标签，展示全部样例用户</span>\n              </div>\n              <div class="tag-chip-row" id="tagQuickSelector"></div>\n            </div>\n          </div>\n        </div>\n\n        <div class="panel">\n          <div class="panel-head">\n            <div>\n              <div class="panel-title">任务链自动编排</div>\n              <div class="panel-subtitle">智能体按预定顺序执行，也可根据隐私、检索、标签等任务重点调整优先级。</div>\n            </div>\n            <div class="status-pill"><span class="dot" id="audioWorkflowDot"></span><span id="audioWorkflowStatus">待执行</span></div>\n          </div>\n          <div class="panel-body">\n            <div class="workflow-chain" id="audioWorkflowChain"></div>\n          </div>\n        </div>\n\n        <section class="tag-dashboard-grid">\n          <div class="tag-subgrid">\n            <div class="panel">\n              <div class="panel-head">\n                <div>\n                  <div class="panel-title">标签体系构建</div>\n                  <div class="panel-subtitle">支持自定义维度和标签版本，当前展示乘用车用户洞察标准体系。</div>\n                </div>\n              </div>\n              <div class="panel-body">\n                <div class="tag-subgrid" id="taxonomyPanel"></div>\n              </div>\n            </div>\n\n            <div class="panel">\n              <div class="panel-head">\n                <div>\n                  <div class="panel-title">比例统计分析</div>\n                  <div class="panel-subtitle">根据当前筛选动态计算单标签用户占比。</div>\n                </div>\n              </div>\n              <div class="panel-body">\n                <div class="stats-list" id="tagStatsPanel"></div>\n              </div>\n            </div>\n          </div>\n\n          <div class="tag-subgrid">\n            <div class="panel">\n              <div class="panel-head">\n                <div>\n                  <div class="panel-title">智能自动标注</div>\n                  <div class="panel-subtitle">对脱敏访谈片段自动识别用户身份、购车动机、痛点、配置诉求和竞品对比。</div>\n                </div>\n              </div>\n              <div class="panel-body">\n                <div class="tag-subgrid" id="taggedRecordPanel"></div>\n              </div>\n            </div>\n\n            <div class="panel">\n              <div class="panel-head">\n                <div>\n                  <div class="panel-title">交叉分析能力</div>\n                  <div class="panel-subtitle">支持多标签组合查询、标签关联分析和潜在关联维度推荐。</div>\n                </div>\n              </div>\n              <div class="panel-body">\n                <div class="tag-subgrid" id="crossAnalysisPanel"></div>\n              </div>\n            </div>\n          </div>\n        </section>\n\n        <div class="panel">\n          <div class="panel-head">\n            <div>\n              <div class="panel-title">从原始音频到高质量数据集</div>\n              <div class="panel-subtitle">把访谈音频变成可训练、可检索、可审计的数据资产，需要同时管好采集、加工、标注、入库和质检。</div>\n            </div>\n          </div>\n          <div class="panel-body">\n            <div class="tag-subgrid" id="datasetPlanPanel"></div>\n          </div>\n        </div>\n      </section>\n\n      <section class="workspace-view report-library" id="reportQueryView" hidden>\n        <div class="panel report-library-panel">\n          <div class="panel-head">\n            <div>\n              <div class="panel-title">报告查询</div>\n              <div class="panel-subtitle">检索 outputs/ppt 中已生成的文字报告、Markdown 报告和 HTML PPT。</div>\n            </div>\n            <button class="btn secondary" id="reportRefreshBtn" type="button">刷新列表</button>\n          </div>\n          <div class="panel-body">\n            <div class="report-searchbar">\n              <input id="reportSearchInput" type="search" placeholder="输入关键词，例如：新能源SUV、15-20万元、用户圈层、区域产能" />\n              <button class="btn" id="reportSearchBtn" type="button">查询报告</button>\n            </div>\n            <div class="report-library-meta" id="reportLibraryMeta">尚未加载报告资产。</div>\n            <div class="report-list" id="reportList">\n              <div class="empty">\n                <div>\n                  <strong>报告资产库</strong>\n                  <p>点击刷新或输入关键词，即可查询已生成的文字版报告和 HTML PPT。</p>\n                </div>\n              </div>\n            </div>\n          </div>\n        </div>\n      </section>\n    </main>\n\n    <aside class="agent-panel">\n      <div class="side-title">AI智能体状态</div>\n      <div class="agent-card active" id="card-market_strategy">\n        <div class="agent-header">\n          <div class="agent-avatar">市</div>\n          <div>\n            <div class="agent-name">市场战略Agent</div>\n            <div class="agent-role">市场分析师</div>\n          </div>\n        </div>\n        <div class="agent-status"><span>状态</span><span id="state-market_strategy">待命</span></div>\n      </div>\n      <div class="agent-card" id="card-user_insight_config">\n        <div class="agent-header">\n          <div class="agent-avatar">配</div>\n          <div>\n            <div class="agent-name">用户洞察Agent</div>\n            <div class="agent-role">用户研究员 / 配置定义</div>\n          </div>\n        </div>\n        <div class="agent-status"><span>状态</span><span id="state-user_insight_config">待命</span></div>\n      </div>\n\n      <div class="side-title">快捷操作</div>\n      <button class="action-btn" data-agent-action="market_strategy">新建市场分析</button>\n      <button class="action-btn" data-agent-action="user_insight_config">生成用户画像</button>\n      <button class="action-btn secondary" id="runAllBtn" type="button">顺序运行双Agent</button>\n\n      <div class="side-title">MCP连接层预留</div>\n      <div class="mcp-grid">\n        <div class="mcp-item"><strong>Database</strong><span>销量、价格、车型配置、库存产能</span></div>\n        <div class="mcp-item"><strong>File Store</strong><span>报告、PPT、Excel、访谈音频库</span></div>\n        <div class="mcp-item"><strong>Vector Store</strong><span>用户原声、评论、转写稿、行业报告</span></div>\n        <div class="mcp-item"><strong>BI</strong><span>销量趋势、区域渗透率、经营指标</span></div>\n        <div class="mcp-item"><strong>Research</strong><span>访谈项目、问卷、受访者画像</span></div>\n      </div>\n\n      <div class="notice">\n        MCP 当前为工程占位，建议由 ToolBase 包装 MCP 调用，保持审计、权限和 2.0.3 稳定版兼容。\n      </div>\n    </aside>\n  </div>\n\n  <script>\n    const $ = (id) => document.getElementById(id);\n    let activeAgent = "market_strategy";\n    let running = false;\n    let lastReport = "";\n    let lastReportMeta = null;\n    let lastQualityGate = null;\n    let progressTimer = null;\n    let executionTimer = null;\n    let audioTagState = {\n      selectedTags: [],\n      taxonomy: {},\n      records: [],\n      statistics: [],\n      crossAnalysis: [],\n      datasetPlan: []\n    };\n\n    const skillDemoMap = {\n      any_search: {\n        agent: "market_strategy",\n        analysisType: "opportunity",\n        question: "请使用 any_search 搜索2026年新能源SUV市场政策、销量和竞争趋势，输出3条可用于市场机会判断的证据，并给出市场进入/暂缓建议。"\n      },\n      web_fetch: {\n        agent: "market_strategy",\n        analysisType: "opportunity",\n        question: "请使用 web_fetch 读取 https://docs.agentscope.io/versions/2.0.3/zh/building-blocks/model ，提炼AgentScope 2.0.3模型配置稳定API要点，并说明如何服务乘用车市场战略研判。"\n      },\n      tavily_search: {\n        agent: "market_strategy",\n        analysisType: "opportunity",\n        question: "请使用 tavily_search 搜索2026年新能源SUV市场政策、销量和竞争趋势，输出3条可用于市场机会判断的证据，并给出市场进入/暂缓建议。"\n      },\n      playwright_scraper: {\n        agent: "market_strategy",\n        analysisType: "risk",\n        question: "请使用 playwright_scraper 抓取公开汽车资讯页面，提取页面标题和关键市场信号；如果当前环境未安装浏览器内核，请说明安装命令和替代分析方案。"\n      },\n      agent_reach_search: {\n        agent: "market_strategy",\n        analysisType: "opportunity",\n        question: "请使用 agent_reach_search 搜索15-20万元新能源SUV市场机会，归纳政策、竞品、用户需求三类证据，并输出业务决策摘要。"\n      },\n      multi_agent: {\n        agent: "market_strategy",\n        analysisType: "",\n        question: "请先由市场战略Agent输出15-20万元新能源SUV机会市场和风险市场判断，再提示可交接给用户洞察Agent生成配置方案。"\n      },\n      user_insight_tagger: {\n        agent: "user_insight_config",\n        analysisType: "user",\n        question: "请调用 user_insight_tagger 处理以下车主原声，并输出用户圈层、需求标签、配置诉求和配置定义建议：我家有两个孩子，后排空间和安全很重要，冬天续航别掉太厉害，最好有热泵和快充；车机要流畅，智驾我可以选装但价格不能太贵。"\n      }\n    };\n\n    const quickMap = {\n      market_strategy: [\n        ["15-20万新能源SUV机会", "请输出精简版业务报告：针对2026年15-20万元新能源SUV乘用车市场，识别2个高潜力增量机会市场和2个高风险衰退预警市场，输出市场分级、区域产能调配建议、进入/暂缓决策意见。每章最多3条要点，必须覆盖全部章节。"],\n        ["华中西南区域机会", "请分析2026年15-20万元新能源SUV在华中和西南市场的机会与风险，输出进入策略和产能调配建议。"],\n        ["风险预警市场", "请识别2026年15-20万元新能源SUV的2个高风险衰退预警市场，说明风险信号、触发条件和暂缓建议。"],\n        ["宏观趋势与政策机会", "请分析2026年乘用车新能源市场宏观趋势和政策机会，并给出15-20万元SUV投放建议。"]\n      ],\n      user_insight_config: [\n        ["目标用户圈层", "请针对15-20万元新能源SUV，锁定3类目标用户圈层，并说明核心购车动机和需求权重。"],\n        ["配置优先级排序", "请针对15-20万元新能源SUV，输出不超过8项整车选装配置优先级排序，并说明标配/选装建议。"],\n        ["家庭用户配置包", "请面向城市品质家庭用户，设计15-20万元新能源SUV的推荐配置包方案和成本约束提示。"],\n        ["年轻用户智能化配置", "请面向新锐首购青年，设计15-20万元新能源SUV智能座舱和智驾配置方案。"]\n      ]\n    };\n\n    const stepsModel = [\n      ["stage1", "意图识别"],\n      ["stage2", "工具与证据准备"],\n      ["stage3", "业务框架推理"],\n      ["stage4", "数据质量门禁"],\n      ["stage5", "报告生成"]\n    ];\n\n    function initSteps() {\n      $("steps").innerHTML = stepsModel.map(([id, name]) => `\n        <div class="step" id="box-${id}">\n          <div class="step-top">\n            <div class="step-name">${name}</div>\n            <div class="step-status" id="status-${id}">待执行</div>\n          </div>\n          <div class="step-summary" id="summary-${id}">-</div>\n        </div>\n      `).join("");\n    }\n\n    function updateStep(stage, status, summary) {\n      const box = $(`box-${stage}`);\n      if (!box) return;\n      box.classList.remove("active", "done");\n      if (status === "running") box.classList.add("active");\n      if (status === "done") box.classList.add("done");\n      $(`status-${stage}`).textContent = status === "done" ? "完成" : status === "running" ? "执行中" : "待执行";\n      if (summary) $(`summary-${stage}`).textContent = summary;\n    }\n\n    function setStatus(kind, text) {\n      $("statusDot").className = "dot " + (kind || "");\n      $("statusText").textContent = text;\n    }\n\n    function appendLog(message) {\n      const now = new Date().toLocaleTimeString();\n      $("eventLog").textContent += `\\n[${now}] ${message}`;\n      $("eventLog").scrollTop = $("eventLog").scrollHeight;\n    }\n\n    function detectExpectedTools(question) {\n      const text = (question || "").toLowerCase();\n      const tools = [];\n      if (text.includes("any_search") || text.includes("anysearch") || text.includes("搜索") || text.includes("销量") || text.includes("政策") || text.includes("趋势")) {\n        tools.push("any_search");\n      }\n      if (text.includes("tavily")) tools.push("tavily_search");\n      if (text.includes("web_fetch") || text.includes("web-fetch") || text.includes("http")) tools.push("web_fetch");\n      if (text.includes("playwright") || text.includes("抓取") || text.includes("爬取")) tools.push("playwright_scraper");\n      if (text.includes("agent_reach") || text.includes("agent-reach")) tools.push("agent_reach_search");\n      if (text.includes("user_insight_tagger") || text.includes("标签") || text.includes("画像") || text.includes("访谈") || text.includes("口碑") || text.includes("车主原声")) {\n        tools.push("user_insight_tagger");\n      }\n      return Array.from(new Set(tools));\n    }\n\n    function buildExecutionPlan(agent, question) {\n      const tools = detectExpectedTools(question);\n      const agentName = agent === "market_strategy" ? "市场战略Agent" : "用户洞察Agent";\n      const toolText = tools.length ? tools.join(", ") : "由模型按任务自动选择";\n      return [\n        {\n          after: 1200,\n          stage: ["stage2", "running", `已装载${agentName}提示词、模型配置与本地工具清单。`],\n          log: `plan: 已识别 ${agentName}，候选工具=${toolText}`\n        },\n        {\n          after: 4200,\n          stage: ["stage2", "running", "正在准备证据检索、用户标签与业务上下文。"],\n          log: "tool-prepare: 等待模型选择是否调用工具；真实调用会在 tool-call 审计日志中出现"\n        },\n        {\n          after: 8200,\n          stage: ["stage3", "running", "正在按市场-产品-用户框架组织分析路径。"],\n          flow: 32,\n          log: agent === "market_strategy"\n            ? "reasoning: 拆解政策信号、销量结构、竞品态势和区域机会"\n            : "reasoning: 拆解用户圈层、购车动机、需求权重和配置映射"\n        },\n        {\n          after: 13200,\n          stage: ["stage3", "running", "正在把证据转换为业务判断与可执行建议。"],\n          flow: 46,\n          log: "reasoning: 生成章节草稿，并校验结论是否能回溯到输入问题"\n        },\n        {\n          after: 20500,\n          stage: ["stage4", "running", "正在检查报告结构、风险提示和证据一致性。"],\n          flow: 62,\n          log: "quality: 检查结论-证据对应关系、章节完整性和业务可读性"\n        },\n        {\n          after: 30500,\n          stage: ["stage5", "running", "正在生成最终报告文本。"],\n          flow: 76,\n          log: "report: 汇总业务分析结果，等待后端返回完整结果"\n        }\n      ];\n    }\n\n    function stopExecutionNarrative() {\n      if (executionTimer) clearInterval(executionTimer);\n      executionTimer = null;\n    }\n\n    function startExecutionNarrative(agent, question) {\n      stopExecutionNarrative();\n      const startedAt = Date.now();\n      const plan = buildExecutionPlan(agent, question);\n      let cursor = 0;\n      let lastHeartbeat = 0;\n\n      appendLog("audit-note: plan/reasoning/quality 是页面阶段提示；tool-call 才是后端真实工具审计");\n\n      executionTimer = setInterval(() => {\n        const elapsed = Date.now() - startedAt;\n        while (cursor < plan.length && elapsed >= plan[cursor].after) {\n          const item = plan[cursor];\n          if (item.stage) updateStep(item.stage[0], item.stage[1], item.stage[2]);\n          if (item.flow) setFlow(item.flow);\n          appendLog(item.log);\n          cursor += 1;\n        }\n        if (elapsed > 36000 && elapsed - lastHeartbeat > 10000) {\n          lastHeartbeat = elapsed;\n          appendLog(`heartbeat: 模型仍在推理或等待工具结果，已耗时 ${Math.round(elapsed / 1000)}s`);\n        }\n      }, 900);\n    }\n\n    function setFlow(percent) {\n      const flow = $("flowFill");\n      if (flow) flow.style.width = `${Math.max(0, Math.min(100, percent))}%`;\n    }\n\n    function resetFlow() {\n      if (progressTimer) clearInterval(progressTimer);\n      progressTimer = null;\n      setFlow(0);\n    }\n\n    function startDemoProgress() {\n      let percent = 8;\n      resetFlow();\n      setFlow(percent);\n      progressTimer = setInterval(() => {\n        percent = Math.min(88, percent + 6);\n        setFlow(percent);\n      }, 700);\n    }\n\n    function finishDemoProgress(success) {\n      if (progressTimer) clearInterval(progressTimer);\n      progressTimer = null;\n      setFlow(success ? 100 : 72);\n    }\n\n    function animateMetric(id, target) {\n      const el = $(id);\n      if (!el) return;\n      const value = Number(target) || 0;\n      const steps = 18;\n      let current = 0;\n      const timer = setInterval(() => {\n        current += 1;\n        el.textContent = String(Math.round(value * current / steps));\n        if (current >= steps) clearInterval(timer);\n      }, 28);\n    }\n\n    function getAgentName(agent) {\n      return agent === "market_strategy" ? "市场战略性机会决策智能体" : "用户洞察与配置定义决策智能体";\n    }\n\n    function buildLeaderReportMarkdown() {\n      const meta = lastReportMeta || {\n        agent: activeAgent,\n        question: $("questionInput").value.trim(),\n        analysisType: $("analysisType").value || "自动识别"\n      };\n      const now = new Date().toLocaleString();\n      return [\n        "# 乘用车市场战略决策AI智能体汇报版",\n        "",\n        `- 生成时间：${now}`,\n        `- 执行智能体：${getAgentName(meta.agent)}`,\n        `- 分析类型：${meta.analysisType || "自动识别"}`,\n        `- 系统能力：双Agent选择、Skill工具链调用、阶段化推理日志、报告生成与下载`,\n        `- 输入问题：${meta.question || "未记录"}`,\n        "",\n        "## 一、决策摘要",\n        "本报告由AgentScope 2.0.3稳定版工程生成，用于支撑乘用车市场战略研判与用户配置定义两个智能体的独立业务分析。",\n        "",\n        "## 二、智能体输出原文",\n        "",\n        lastReport || "暂无报告内容。"\n      ].join("\\n");\n    }\n\n    function buildPptMarkdown() {\n      if (!lastReport) return "";\n      const title = "# 乘用车市场战略决策AI智能体PPT";\n      if (/^#\\s+/m.test(lastReport)) return lastReport;\n      return `${title}\\n\\n${lastReport}`;\n    }\n\n    function renderLeaderReport() {\n      if (!lastReport) {\n        alert("请先运行一次智能体分析，再生成汇报版。");\n        return;\n      }\n      const meta = lastReportMeta || { agent: activeAgent, analysisType: $("analysisType").value || "自动识别" };\n      const cover = `\n        <div class="report-cover">\n          <div class="report-cover-title">乘用车市场战略决策AI智能体汇报版</div>\n          <div class="report-cover-meta">智能体：${getAgentName(meta.agent)}\u3000｜\u3000生成时间：${new Date().toLocaleString()}</div>\n          <div class="report-cover-meta">能力展示：双Agent选择 / Skill工具链 / 阶段日志 / 报告生成下载</div>\n        </div>\n      `;\n      $("reportArea").innerHTML = cover + markdownToHtml(lastReport);\n      appendLog("ppt-report: HTML PPT 已生成");\n    }\n\n    function downloadText(filename, text) {\n      const blob = new Blob([text], { type: "text/markdown;charset=utf-8" });\n      const url = URL.createObjectURL(blob);\n      const link = document.createElement("a");\n      link.href = url;\n      link.download = filename;\n      document.body.appendChild(link);\n      link.click();\n      link.remove();\n      URL.revokeObjectURL(url);\n    }\n\n    async function generatePptReport() {\n      if (!lastReport) {\n        alert("请先运行一次智能体分析，再生成PPT版。");\n        return;\n      }\n      if (lastQualityGate && ["风险", "阻断"].includes(lastQualityGate.overall_status)) {\n        const ok = confirm(`质量门禁状态为“${lastQualityGate.overall_status}”。建议先补充数据来源、口径或证据链。是否仍生成PPT草稿？`);\n        if (!ok) return;\n      }\n      $("leaderReportBtn").disabled = true;\n      $("leaderReportBtn").textContent = "生成中";\n      appendLog("ppt: start consulting html-ppt builder");\n      try {\n        const res = await fetch("/api/ppt", {\n          method: "POST",\n          headers: { "Content-Type": "application/json" },\n          body: JSON.stringify({\n            markdown: buildPptMarkdown(),\n            title: "乘用车市场战略决策AI智能体PPT"\n          })\n        });\n        const data = await res.json();\n        if (!res.ok || data.error || data.success === false) throw new Error(data.error || `HTTP ${res.status}`);\n        const links = `\n          <div class="report-cover">\n            <div class="report-cover-title">HTML PPT 已生成</div>\n            <div class="report-cover-meta">页数：${data.slides_count || "-"}\u3000｜\u3000文件大小：${data.file_size || 0} bytes</div>\n            <p><a href="${data.html_url}" target="_blank">打开PPT</a>\u3000<a href="${data.html_url}" download>下载HTML</a></p>\n          </div>\n        `;\n        $("reportArea").innerHTML = links + markdownToHtml(lastReport);\n        appendLog(`ppt: generated ${data.html_url} / engine=${data.engine || "unknown"}`);\n      } catch (err) {\n        stopExecutionNarrative();\n        appendLog(`ppt-error: ${err.message}`);\n        alert(`生成PPT失败：${err.message}`);\n      } finally {\n        stopExecutionNarrative();\n        $("leaderReportBtn").disabled = false;\n        $("leaderReportBtn").textContent = "生成PPT版";\n      }\n    }\n\n    function setAgent(agent) {\n      activeAgent = agent;\n      document.querySelectorAll(".agent-tab").forEach(btn => btn.classList.toggle("active", btn.dataset.agent === agent));\n      document.querySelectorAll(".agent-card").forEach(card => card.classList.remove("active"));\n      const target = $(`card-${agent}`);\n      if (target) target.classList.add("active");\n      $("mAgent").textContent = agent === "market_strategy" ? "市场" : "配置";\n      renderQuick();\n    }\n\n    function renderQuick() {\n      $("quickGrid").innerHTML = quickMap[activeAgent].map(([label, q]) => `\n        <button class="quick" type="button" data-q="${escapeAttr(q)}">${label}</button>\n      `).join("");\n      document.querySelectorAll(".quick").forEach(btn => {\n        btn.addEventListener("click", () => { $("questionInput").value = btn.dataset.q; });\n      });\n      if (!$("questionInput").value.trim()) {\n        $("questionInput").value = quickMap[activeAgent][0][1];\n      }\n    }\n\n    function escapeAttr(text) {\n      return text.replaceAll("&", "&amp;").replaceAll(\'"\', "&quot;").replaceAll("<", "&lt;").replaceAll(">", "&gt;");\n    }\n\n    function markdownToHtml(markdown) {\n      const safe = (markdown || "")\n        .replace(/&/g, "&amp;")\n        .replace(/</g, "&lt;")\n        .replace(/>/g, "&gt;");\n\n      const lines = safe.split(/\\r?\\n/);\n      let html = "";\n      let inTable = false;\n      for (const line of lines) {\n        if (/^\\|.+\\|$/.test(line.trim())) {\n          const cells = line.trim().slice(1, -1).split("|").map(c => c.trim());\n          if (cells.every(c => /^:?-{3,}:?$/.test(c))) continue;\n          if (!inTable) { html += "<table>"; inTable = true; }\n          html += "<tr>" + cells.map(c => `<td>${formatInline(c)}</td>`).join("") + "</tr>";\n          continue;\n        }\n        if (inTable) { html += "</table>"; inTable = false; }\n        if (/^###\\s+/.test(line)) html += `<h3>${formatInline(line.replace(/^###\\s+/, ""))}</h3>`;\n        else if (/^##\\s+/.test(line)) html += `<h2>${formatInline(line.replace(/^##\\s+/, ""))}</h2>`;\n        else if (/^#\\s+/.test(line)) html += `<h1>${formatInline(line.replace(/^#\\s+/, ""))}</h1>`;\n        else if (/^[-*]\\s+/.test(line)) html += `<p>• ${formatInline(line.replace(/^[-*]\\s+/, ""))}</p>`;\n        else if (line.trim()) html += `<p>${formatInline(line)}</p>`;\n      }\n      if (inTable) html += "</table>";\n      return html || `<div class="empty"><div><strong>没有返回内容</strong><p>请调整问题后重试。</p></div></div>`;\n    }\n\n    function formatInline(text) {\n      return text.replace(/\\*\\*(.*?)\\*\\*/g, "<strong>$1</strong>");\n    }\n\n    async function refreshHealth() {\n      try {\n        const res = await fetch("/api/config");\n        const data = await res.json();\n        $("provider").textContent = data.active_provider;\n        $("model").textContent = data.model;\n        $("endpoint").textContent = data.endpoint;\n        setStatus("ok", "后端已连接");\n      } catch (err) {\n        setStatus("bad", "后端未连接");\n      }\n    }\n\n    function flattenTaxonomy(taxonomy) {\n      return Object.values(taxonomy || {}).flat();\n    }\n\n    function updateAudioWorkflowStatus(kind, text) {\n      $("audioWorkflowDot").className = "dot " + (kind || "");\n      $("audioWorkflowStatus").textContent = text;\n    }\n\n    function updateTagSelectionMeta() {\n      const tags = audioTagState.selectedTags;\n      $("tagSelectionMeta").textContent = tags.length\n        ? `已选择 ${tags.length} 个标签：${tags.join(" + ")}`\n        : "未选择标签，展示全部样例用户";\n    }\n\n    function renderTagQuickSelector() {\n      const labels = flattenTaxonomy(audioTagState.taxonomy);\n      $("tagQuickSelector").innerHTML = labels.map(label => `\n        <button class="tag-token ${audioTagState.selectedTags.includes(label) ? "active" : ""}" data-tag="${escapeAttr(label)}" type="button">${label}</button>\n      `).join("");\n      document.querySelectorAll("#tagQuickSelector .tag-token").forEach(btn => {\n        btn.addEventListener("click", () => {\n          const tag = btn.dataset.tag;\n          if (audioTagState.selectedTags.includes(tag)) {\n            audioTagState.selectedTags = audioTagState.selectedTags.filter(item => item !== tag);\n          } else {\n            audioTagState.selectedTags.push(tag);\n          }\n          updateTagSelectionMeta();\n          runAudioWorkflow(false);\n        });\n      });\n      updateTagSelectionMeta();\n    }\n\n    function renderTaxonomyPanel() {\n      const taxonomy = audioTagState.taxonomy || {};\n      $("taxonomyPanel").innerHTML = Object.entries(taxonomy).map(([category, tags]) => `\n        <div class="taxonomy-box">\n          <div class="taxonomy-title">${escapeAttr(category)}</div>\n          <div class="tag-chip-row">\n            ${tags.map(tag => `<span class="tag-token ${audioTagState.selectedTags.includes(tag) ? "active" : ""}">${escapeAttr(tag)}</span>`).join("")}\n          </div>\n        </div>\n      `).join("");\n    }\n\n    function renderWorkflowChain(workflow, activeIndex = workflow.length - 1) {\n      $("audioWorkflowChain").innerHTML = (workflow || []).map((item, index) => `\n        <div class="workflow-step-card ${index <= activeIndex ? "active" : ""}">\n          <div class="workflow-step-index">${String(index + 1).padStart(2, "0")}</div>\n          <div class="workflow-step-name">${escapeAttr(item.step)}</div>\n          <div class="workflow-step-output">${escapeAttr(item.output)}</div>\n        </div>\n      `).join("");\n    }\n\n    function animateWorkflowChain(workflow) {\n      if (!workflow || !workflow.length) return;\n      let index = 0;\n      renderWorkflowChain(workflow, 0);\n      const timer = setInterval(() => {\n        index += 1;\n        renderWorkflowChain(workflow, Math.min(index, workflow.length - 1));\n        if (index >= workflow.length - 1) clearInterval(timer);\n      }, 140);\n    }\n\n    function renderTaggedRecords() {\n      const records = audioTagState.records || [];\n      if (!records.length) {\n        $("taggedRecordPanel").innerHTML = `<div class="tag-empty-note">当前标签组合没有命中样例用户。可以减少筛选标签，或在后续接入更多访谈样本后重新计算。</div>`;\n        return;\n      }\n      $("taggedRecordPanel").innerHTML = records.map(record => `\n        <article class="tag-record-card">\n          <div class="report-item-head">\n            <div class="report-name">${escapeAttr(record.user_id)} · ${escapeAttr(record.audio_name)}</div>\n            <span class="report-type">${escapeAttr(record.region)} / ${escapeAttr(record.city_tier)}</span>\n          </div>\n          <div class="tag-record-meta">\n            <span>时长：${escapeAttr(record.duration)}</span>\n            <span>说话人：${record.speaker_count}</span>\n            <span>命中标签：${record.labels.length}</span>\n          </div>\n          <div class="tag-record-text">${escapeAttr(record.evidence)}</div>\n          <div class="tag-chip-row">${record.labels.slice(0, 12).map(tag => `<span class="tag-token active">${escapeAttr(tag)}</span>`).join("")}</div>\n          <div class="tag-score-line"><span>情绪强度</span><div class="mini-bar"><span style="width:${record.emotion_score}%"></span></div><span>${record.emotion_score}</span></div>\n          <div class="tag-score-line"><span>需求强度</span><div class="mini-bar"><span style="width:${record.intensity_score}%"></span></div><span>${record.intensity_score}</span></div>\n          <div class="tag-empty-note">配置映射：${escapeAttr((record.config_mapping || []).join("；") || "暂无明确配置映射")}</div>\n        </article>\n      `).join("");\n    }\n\n    function renderTagStats() {\n      const stats = audioTagState.statistics || [];\n      if (!stats.length) {\n        $("tagStatsPanel").innerHTML = `<div class="tag-empty-note">暂无统计结果。请执行编排或调整标签筛选。</div>`;\n        return;\n      }\n      $("tagStatsPanel").innerHTML = stats.map(item => `\n        <div class="stat-row">\n          <span>${escapeAttr(item.tag)}</span>\n          <div class="stat-bar"><span style="width:${Math.min(item.ratio, 100)}%"></span></div>\n          <strong>${item.ratio}%</strong>\n        </div>\n      `).join("");\n    }\n\n    function renderCrossAnalysis() {\n      const cross = audioTagState.crossAnalysis || [];\n      if (!cross.length) {\n        $("crossAnalysisPanel").innerHTML = `<div class="tag-empty-note">当前样本下未形成高频标签组合。建议放宽筛选，或接入更多访谈片段后再做关联发现。</div>`;\n        return;\n      }\n      $("crossAnalysisPanel").innerHTML = cross.map(item => `\n        <div class="tag-insight-card">\n          <div class="report-item-head">\n            <div class="report-name">${escapeAttr(item.pair)}</div>\n            <span class="report-type">${item.count}次</span>\n          </div>\n          <div class="report-snippet">${escapeAttr(item.insight)}</div>\n        </div>\n      `).join("");\n    }\n\n    function renderDatasetPlan() {\n      const plan = audioTagState.datasetPlan || [];\n      $("datasetPlanPanel").innerHTML = plan.map((item, index) => `\n        <div class="dataset-row">\n          <div class="workflow-step-index">${String(index + 1).padStart(2, "0")}</div>\n          <div class="report-snippet">${escapeAttr(item)}</div>\n        </div>\n      `).join("");\n    }\n\n    function renderAudioTagWorkbench(data, animate = true) {\n      audioTagState.taxonomy = data.taxonomy || {};\n      audioTagState.records = data.records || [];\n      audioTagState.statistics = data.statistics || [];\n      audioTagState.crossAnalysis = data.cross_analysis || [];\n      audioTagState.datasetPlan = data.dataset_plan || [];\n      renderTagQuickSelector();\n      renderTaxonomyPanel();\n      if (animate) animateWorkflowChain(data.workflow || []);\n      else renderWorkflowChain(data.workflow || []);\n      renderTaggedRecords();\n      renderTagStats();\n      renderCrossAnalysis();\n      renderDatasetPlan();\n    }\n\n    async function runAudioWorkflow(animate = true) {\n      updateAudioWorkflowStatus("run", "编排执行中");\n      $("runAudioWorkflowBtn").disabled = true;\n      try {\n        const res = await fetch("/api/audio-tags/run", {\n          method: "POST",\n          headers: { "Content-Type": "application/json" },\n          body: JSON.stringify({\n            instruction: $("audioWorkflowInput").value.trim(),\n            selected_tags: audioTagState.selectedTags\n          })\n        });\n        const data = await res.json();\n        if (!res.ok || data.error) throw new Error(data.error || `HTTP ${res.status}`);\n        renderAudioTagWorkbench(data, animate);\n        updateAudioWorkflowStatus("ok", `完成：${data.filtered_count}/${data.all_record_count} 条样本命中`);\n        $("tagWorkbenchView").dataset.loaded = "true";\n      } catch (err) {\n        updateAudioWorkflowStatus("bad", "执行失败");\n        $("taggedRecordPanel").innerHTML = `<div class="tag-empty-note">执行失败：${escapeAttr(err.message)}</div>`;\n      } finally {\n        $("runAudioWorkflowBtn").disabled = false;\n      }\n    }\n\n    function exportTagCsv() {\n      const records = audioTagState.records || [];\n      if (!records.length) {\n        alert("当前没有可导出的标签结果。");\n        return;\n      }\n      const header = ["user_id", "audio_name", "region", "city_tier", "labels", "emotion_score", "intensity_score", "config_mapping", "evidence"];\n      const rows = records.map(record => header.map(key => {\n        const value = Array.isArray(record[key]) ? record[key].join("|") : record[key];\n        return `"${String(value || "").replaceAll(\'"\', \'""\')}"`;\n      }).join(","));\n      downloadText(`用户标签分析结果-${new Date().toISOString().slice(0, 10)}.csv`, [header.join(","), ...rows].join("\\n"));\n    }\n\n    function switchMainView(view, activeNav = null) {\n      const targetMap = {\n        reports: "reportQueryView",\n        tagging: "tagWorkbenchView",\n        analysis: "analysisView"\n      };\n      const target = targetMap[view] || "analysisView";\n      document.querySelectorAll(".workspace-view").forEach(item => {\n        item.hidden = item.id !== target;\n      });\n      document.querySelectorAll(".nav-item").forEach(item => item.classList.remove("active"));\n      (activeNav || document.querySelector(`.nav-item[data-view="${view}"]`))?.classList.add("active");\n      if (view === "reports" && !$("reportList").dataset.loaded) {\n        loadReportLibrary();\n      }\n      if (view === "tagging" && !$("tagWorkbenchView").dataset.loaded) {\n        runAudioWorkflow();\n      }\n    }\n\n    function formatFileSize(bytes) {\n      if (!Number.isFinite(bytes)) return "-";\n      if (bytes < 1024) return `${bytes} B`;\n      if (bytes < 1024 * 1024) return `${(bytes / 1024).toFixed(1)} KB`;\n      return `${(bytes / 1024 / 1024).toFixed(1)} MB`;\n    }\n\n    async function loadReportLibrary(queryValue = null) {\n      const query = queryValue === null ? $("reportSearchInput").value.trim() : queryValue;\n      if (queryValue !== null) $("reportSearchInput").value = queryValue;\n      $("reportList").dataset.loaded = "true";\n      $("reportLibraryMeta").textContent = "正在检索报告资产...";\n      $("reportList").innerHTML = `<div class="empty"><div><strong>正在查询</strong><p>正在扫描 outputs/ppt 报告目录。</p></div></div>`;\n      try {\n        const res = await fetch(`/api/reports?q=${encodeURIComponent(query)}`);\n        const data = await res.json();\n        if (!res.ok || data.error) throw new Error(data.error || `HTTP ${res.status}`);\n        renderReportLibrary(data);\n      } catch (err) {\n        $("reportLibraryMeta").textContent = "报告查询失败";\n        $("reportList").innerHTML = `<div class="empty"><div><strong>查询失败</strong><p>${escapeAttr(err.message)}</p></div></div>`;\n      }\n    }\n\n    function renderReportLibrary(data) {\n      const items = data.items || [];\n      const query = data.query || "";\n      $("reportLibraryMeta").textContent = query\n        ? `关键词“${query}”命中 ${items.length} 份报告，已按最近修改时间排序。`\n        : `共发现 ${items.length} 份可用报告，已按最近修改时间排序。`;\n      if (!items.length) {\n        $("reportList").innerHTML = `<div class="empty"><div><strong>没有找到匹配报告</strong><p>可以换成车型、价格带、区域、用户圈层等关键词再试。</p></div></div>`;\n        return;\n      }\n      $("reportList").innerHTML = items.map(item => {\n        const openText = item.ext === ".html" ? "打开PPT" : "打开报告";\n        return `\n          <article class="report-item">\n            <div class="report-item-head">\n              <div class="report-name">${escapeAttr(item.name)}</div>\n              <span class="report-type">${escapeAttr(item.type)}</span>\n            </div>\n            <div class="report-snippet">${escapeAttr(item.snippet || "暂无正文摘要")}</div>\n            <div class="report-file-meta">\n              <span>修改时间：${escapeAttr(item.modified)}</span>\n              <span>大小：${formatFileSize(item.size)}</span>\n              <span>路径：${escapeAttr(item.relative_path)}</span>\n            </div>\n            <div class="report-actions">\n              <a class="link-btn primary" href="${escapeAttr(item.url)}" target="_blank" rel="noreferrer">${openText}</a>\n              <a class="link-btn" href="${escapeAttr(item.url)}" download>下载文件</a>\n            </div>\n          </article>\n        `;\n      }).join("");\n    }\n\n\n    function renderQualityGate(data) {\n      lastQualityGate = data;\n      const checks = (data.checks || []).map(item => `\n        <div class="quality-item">\n          <div class="quality-item-head">\n            <div class="quality-name">${escapeAttr(item.name)}</div>\n            <span class="quality-badge ${item.status}">${escapeAttr(item.status)}</span>\n          </div>\n          <div class="quality-detail">${escapeAttr(item.evidence || "")}</div>\n          <div class="quality-detail">建议：${escapeAttr(item.suggestion || "")}</div>\n        </div>\n      `).join("");\n      $("qualityGatePanel").innerHTML = `\n        <div class="quality-summary">\n          <div>\n            <strong>数据质量门禁</strong>\n            <div class="panel-subtitle">${escapeAttr(data.impact || "")}</div>\n          </div>\n          <div>\n            <span class="quality-status ${data.overall_status}">${escapeAttr(data.overall_status)}</span>\n            <div class="panel-subtitle">评分 ${data.score} / 100</div>\n          </div>\n        </div>\n        <div class="quality-grid">${checks}</div>\n        <div class="quality-actions"><strong>建议动作：</strong>${(data.suggested_actions || []).map(escapeAttr).join("；")}</div>\n      `;\n      $("mQuality").textContent = data.overall_status || "-";\n      $("mQuality").style.color = data.overall_status === "通过"\n        ? "var(--success)"\n        : data.overall_status === "待核验"\n          ? "#B45309"\n          : "var(--danger)";\n    }\n\n    async function evaluateQualityGate(report, question, toolEvents) {\n      const res = await fetch("/api/quality-gate", {\n        method: "POST",\n        headers: { "Content-Type": "application/json" },\n        body: JSON.stringify({ report, question, tool_events: toolEvents || [] })\n      });\n      const data = await res.json();\n      if (!res.ok || data.error) throw new Error(data.error || `HTTP ${res.status}`);\n      renderQualityGate(data);\n      return data;\n    }\n\n    async function runAnalysis(agent = activeAgent, question = $("questionInput").value.trim()) {\n      if (running) return;\n      if (!question) { alert("请输入分析问题"); return; }\n      setAgent(agent);\n      running = true;\n      $("runBtn").disabled = true;\n      $("runBtn").textContent = "分析中";\n      $(`state-${agent}`).textContent = "运行中";\n      setStatus("run", "智能体运行中");\n      $("eventLog").textContent = "开始接收智能体执行日志...";\n      $("reportArea").innerHTML = `<div class="empty"><div><strong>智能体正在分析</strong><p>请观察左侧阶段进度。</p></div></div>`;\n      $("mQuality").textContent = "-";\n      $("mLength").textContent = "-";\n      initSteps();\n      startDemoProgress();\n\n      updateStep("stage1", "done", "已识别业务场景与目标智能体。");\n      updateStep("stage2", "running", "正在准备模型、工具与业务上下文。");\n      appendLog(`Agent=${agent}`);\n      startExecutionNarrative(agent, question);\n\n      try {\n        const res = await fetch("/api/chat", {\n          method: "POST",\n          headers: { "Content-Type": "application/json" },\n          body: JSON.stringify({ agent, question, analysis_type: $("analysisType").value })\n        });\n        const data = await res.json();\n        if (!res.ok || data.error) throw new Error(data.error || `HTTP ${res.status}`);\n        stopExecutionNarrative();\n\n        updateStep("stage2", "done", "工具链准备完成。");\n        setFlow(58);\n        updateStep("stage3", "done", "已按业务框架完成推理。");\n        setFlow(76);\n        updateStep("stage4", "running", "正在执行汽车咨询报告数据质量门禁。");\n        setFlow(82);\n        appendLog("complete: report returned");\n        if (Array.isArray(data.tool_events) && data.tool_events.length) {\n          appendLog(`audit: 后端返回真实工具审计 ${data.tool_events.length} 条`);\n          data.tool_events.forEach(item => appendLog(`tool-call: ${item.name} / ${item.status} / ${item.detail}`));\n        } else {\n          appendLog("audit: 后端本轮未捕获到真实工具调用");\n        }\n\n        lastReport = data.answer || "";\n        lastReportMeta = {\n          agent,\n          question,\n          analysisType: $("analysisType").value || "自动识别",\n          createdAt: new Date().toISOString()\n        };\n        $("reportArea").innerHTML = markdownToHtml(lastReport);\n        const gate = await evaluateQualityGate(lastReport, question, data.tool_events || []);\n        updateStep("stage4", "done", `门禁状态=${gate.overall_status}，通过${gate.pass_count}/8项。`);\n        setFlow(92);\n        updateStep("stage5", "done", "完整报告已返回，质量结果已附加。");\n        animateMetric("mLength", lastReport.length);\n        finishDemoProgress(true);\n        setStatus(gate.overall_status === "通过" ? "ok" : gate.overall_status === "待核验" ? "run" : "bad", `质量门禁：${gate.overall_status}`);\n      } catch (err) {\n        updateStep("stage2", "done", "请求已发送。");\n        updateStep("stage3", "running", "执行失败，请查看错误。");\n        stopExecutionNarrative();\n        appendLog(`ERROR: ${err.message}`);\n        $("reportArea").innerHTML = `<div class="empty"><div><strong>分析失败</strong><p>${err.message}</p></div></div>`;\n        finishDemoProgress(false);\n        setStatus("bad", "分析失败");\n      } finally {\n        running = false;\n        stopExecutionNarrative();\n        $("runBtn").disabled = false;\n        $("runBtn").textContent = "开始分析";\n        $(`state-${agent}`).textContent = "待命";\n      }\n    }\n\n    document.querySelectorAll("[data-view]").forEach(btn => {\n      btn.addEventListener("click", () => switchMainView(btn.dataset.view, btn));\n    });\n\n    $("reportSearchBtn").addEventListener("click", () => loadReportLibrary());\n    $("reportRefreshBtn").addEventListener("click", () => loadReportLibrary(""));\n    $("reportSearchInput").addEventListener("keydown", (event) => {\n      if (event.key === "Enter") loadReportLibrary();\n    });\n    $("runAudioWorkflowBtn").addEventListener("click", () => runAudioWorkflow());\n    $("tagResetBtn").addEventListener("click", () => {\n      audioTagState.selectedTags = [];\n      updateTagSelectionMeta();\n      runAudioWorkflow(false);\n    });\n    $("exportTagCsvBtn").addEventListener("click", exportTagCsv);\n\n    document.querySelectorAll(".agent-tab").forEach(btn => {\n      btn.addEventListener("click", () => {\n        setAgent(btn.dataset.agent);\n        $("questionInput").value = quickMap[btn.dataset.agent][0][1];\n      });\n    });\n\n    document.querySelectorAll(".scene-card").forEach(card => {\n      card.addEventListener("click", () => {\n        document.querySelectorAll(".scene-card").forEach(c => c.classList.remove("active"));\n        card.classList.add("active");\n        setAgent(card.dataset.agent);\n        $("questionInput").value = card.dataset.q;\n      });\n    });\n\n    document.querySelectorAll("[data-agent-action]").forEach(btn => {\n      btn.addEventListener("click", () => {\n        const agent = btn.dataset.agentAction;\n        setAgent(agent);\n        $("questionInput").value = quickMap[agent][0][1];\n      });\n    });\n\n    $("runBtn").addEventListener("click", () => runAnalysis());\n    $("healthBtn").addEventListener("click", refreshHealth);\n    $("copyBtn").addEventListener("click", async () => {\n      if (!lastReport) return;\n      await navigator.clipboard.writeText(lastReport);\n      appendLog("报告已复制到剪贴板");\n    });\n    $("leaderReportBtn").addEventListener("click", generatePptReport);\n    $("downloadMdBtn").addEventListener("click", () => {\n      if (!lastReport) {\n        alert("请先运行一次智能体分析，再下载报告。");\n        return;\n      }\n      const stamp = new Date().toISOString().slice(0, 19).replace(/[:T]/g, "-");\n      downloadText(`乘用车市场战略决策AI智能体汇报版-${stamp}.md`, buildLeaderReportMarkdown());\n      appendLog("download: markdown report exported");\n    });\n    document.querySelectorAll(".skill-chip").forEach(btn => {\n      btn.addEventListener("click", () => {\n        const demo = skillDemoMap[btn.dataset.skill];\n        if (!demo) return;\n        setAgent(demo.agent);\n        $("analysisType").value = demo.analysisType || "";\n        $("questionInput").value = demo.question;\n        document.querySelectorAll(".skill-chip").forEach(item => item.classList.remove("active"));\n        btn.classList.add("active");\n        appendLog(`skill-task: ${btn.textContent.trim()} 已填入任务问题`);\n        if (btn.dataset.skill === "multi_agent") {\n          appendLog("tip: 可点击右侧“顺序运行双Agent”展示市场Agent与配置Agent独立协作流程");\n        }\n      });\n    });\n    $("runAllBtn").addEventListener("click", async () => {\n      await runAnalysis("market_strategy", quickMap.market_strategy[0][1]);\n      if (!running) await runAnalysis("user_insight_config", quickMap.user_insight_config[0][1]);\n    });\n\n    $("questionInput").addEventListener("keydown", (event) => {\n      if (event.key === "Enter" && (event.ctrlKey || event.metaKey)) runAnalysis();\n    });\n\n    initSteps();\n    renderQuick();\n    $("questionInput").value = quickMap.market_strategy[0][1];\n    refreshHealth();\n  </script>\n</body>\n</html>\n'


class AgentDemoHandler(BaseHTTPRequestHandler):
    """极简 HTTP 服务，避免引入额外 Web 框架。"""

    server_version = "AgentScopeAutoDecisionDemo/0.2"

    def do_GET(self) -> None:
        parsed = urlparse(self.path)
        request_path = parsed.path

        if request_path == "/":
            self._send_html(HTML)
            return

        if request_path.startswith("/outputs/") or request_path.startswith("/skills/html-ppt-skill/assets/"):
            self._send_file(request_path)
            return

        if request_path == "/api/config":
            config = load_json_config()
            provider_name = config.get("active_provider", "")
            provider = config.get("providers", {}).get(provider_name, {})
            self._send_json({
                "active_provider": provider_name,
                "model": expand_env(provider.get("model", "")),
                "endpoint": expand_env(provider.get("base_url") or provider.get("host") or ""),
                "agents": config.get("agents", {}),
            })
            return

        if request_path == "/api/reports":
            params = parse_qs(parsed.query)
            query = params.get("q", [""])[0]
            self._send_json(list_generated_reports(query))
            return

        if request_path == "/api/audio-tags/config":
            self._send_json({"taxonomy": DEFAULT_TAG_TAXONOMY})
            return

        if request_path == "/api/quality-gate/rules":
            self._send_json({"rules": QUALITY_RULES})
            return

        self.send_error(HTTPStatus.NOT_FOUND)

    def do_POST(self) -> None:
        if self.path not in {"/api/chat", "/api/ppt", "/api/audio-tags/run", "/api/quality-gate"}:
            self.send_error(HTTPStatus.NOT_FOUND)
            return

        length = int(self.headers.get("Content-Length", "0"))
        body = self.rfile.read(length).decode("utf-8") if length else "{}"
        payload = json.loads(body or "{}")

        try:
            if self.path == "/api/audio-tags/run":
                instruction = str(payload.get("instruction", "")).strip()
                selected_tags = payload.get("selected_tags", [])
                if not isinstance(selected_tags, list):
                    selected_tags = []
                selected_tags = [str(tag) for tag in selected_tags]
                result = run_audio_tagging_workflow(
                    instruction or "执行访谈类音频处理工作流，并生成用户标签洞察。",
                    selected_tags,
                )
                self._send_json(result)
                return

            if self.path == "/api/quality-gate":
                tool_events = payload.get("tool_events", [])
                if not isinstance(tool_events, list):
                    tool_events = []
                self._send_json(evaluate_report_quality(
                    str(payload.get("report", "")),
                    str(payload.get("question", "")),
                    tool_events,
                ))
                return

            if self.path == "/api/ppt":
                markdown = str(payload.get("markdown", "")).strip()
                title = str(payload.get("title", "乘用车市场战略决策AI智能体PPT")).strip()
                if not markdown:
                    self._send_json({"error": "PPT Markdown 内容不能为空。"}, status=400)
                    return
                result = generate_ppt_from_markdown(markdown, title)
                self._send_json(result)
                return

            question = str(payload.get("question", "")).strip()
            agent_key = str(payload.get("agent", "market_strategy")).strip() or "market_strategy"
            if not question:
                self._send_json({"error": "问题不能为空。"}, status=400)
                return
            # AgentScope 2.0.3 的本地工具审计事件由 tools.custom_tools 暂存；
            # 串行化 chat 请求可避免并发演示时不同请求的工具日志相互串线。
            with AGENT_RUN_LOCK:
                result = asyncio.run(run_agent_once(agent_key, question))
            self._send_json(result)
        except AuthenticationError:
            self._send_json({
                "error": "MiniMax/OpenAI 兼容接口鉴权失败：请检查 .env 中的 API Key 是否有效、是否属于 https://api.minimaxi.com/v1 对应的平台。"
            }, status=401)
        except Exception as exc:
            self._send_json({"error": type(exc).__name__ + ": " + str(exc)}, status=500)

    def log_message(self, fmt: str, *args: Any) -> None:
        print("[web] " + self.address_string() + " - " + (fmt % args))

    def _send_html(self, html: str) -> None:
        data = html.encode("utf-8")
        self.send_response(HTTPStatus.OK)
        self.send_header("Content-Type", "text/html; charset=utf-8")
        self.send_header("Content-Length", str(len(data)))
        self.end_headers()
        self.wfile.write(data)

    def _send_file(self, request_path: str) -> None:
        """只允许访问 outputs 与 html-ppt-skill 静态资产。"""
        relative = unquote(request_path).lstrip("/").replace("\\", "/")
        output_root = (PROJECT_DIR / "outputs").resolve()
        skill_asset_root = (PROJECT_DIR / "skills" / "html-ppt-skill" / "assets").resolve()
        file_path = (PROJECT_DIR / relative).resolve()
        allowed = False
        try:
            if output_root in file_path.parents:
                allowed = True
            if skill_asset_root in file_path.parents:
                allowed = True
        except Exception:
            allowed = False

        if not allowed or not file_path.is_file():
            self.send_error(HTTPStatus.NOT_FOUND)
            return

        suffix = file_path.suffix.lower()
        content_type_map = {
            ".html": "text/html; charset=utf-8",
            ".css": "text/css; charset=utf-8",
            ".js": "application/javascript; charset=utf-8",
            ".json": "application/json; charset=utf-8",
            ".png": "image/png",
            ".jpg": "image/jpeg",
            ".jpeg": "image/jpeg",
            ".gif": "image/gif",
            ".svg": "image/svg+xml",
        }
        content_type = content_type_map.get(suffix, "text/plain; charset=utf-8")
        data = file_path.read_bytes()
        self.send_response(HTTPStatus.OK)
        self.send_header("Content-Type", content_type)
        self.send_header("Content-Length", str(len(data)))
        self.end_headers()
        self.wfile.write(data)

    def _send_json(self, payload: dict[str, Any], status: int = 200) -> None:
        data = json.dumps(payload, ensure_ascii=False).encode("utf-8")
        self.send_response(status)
        self.send_header("Content-Type", "application/json; charset=utf-8")
        self.send_header("Content-Length", str(len(data)))
        self.end_headers()
        self.wfile.write(data)


def extract_report_search_text(path: Path) -> str:
    """提取报告正文片段，用于业务侧模糊检索。

    这里不解析完整 PPT 结构，只抽取可读文本，保持 Demo 足够轻量；
    后续可替换为 SQLite 全文索引、向量检索或 MCP 文件库检索。
    """
    raw = path.read_text(encoding="utf-8", errors="ignore")[:160000]
    if path.suffix.lower() == ".html":
        raw = re.sub(r"<script[\s\S]*?</script>", " ", raw, flags=re.IGNORECASE)
        raw = re.sub(r"<style[\s\S]*?</style>", " ", raw, flags=re.IGNORECASE)
        raw = re.sub(r"<[^>]+>", " ", raw)
    return re.sub(r"\s+", " ", raw).strip()


def build_report_snippet(text: str, keyword: str) -> str:
    """根据关键词生成一段命中摘要，便于业务人员快速判断是否打开报告。"""
    if not text:
        return ""
    if not keyword:
        return text[:180]
    lower_text = text.lower()
    lower_keyword = keyword.lower()
    index = lower_text.find(lower_keyword)
    if index < 0:
        return text[:180]
    start = max(0, index - 70)
    end = min(len(text), index + len(keyword) + 130)
    prefix = "..." if start > 0 else ""
    suffix = "..." if end < len(text) else ""
    return prefix + text[start:end] + suffix


def list_generated_reports(query: str) -> dict[str, Any]:
    """扫描 outputs/ppt 下的历史报告，返回前端报告资产库列表。"""
    keyword = query.strip()
    items: list[dict[str, Any]] = []
    if not PPT_OUTPUT_DIR.exists():
        return {"query": keyword, "items": items}

    for path in PPT_OUTPUT_DIR.rglob("*"):
        suffix = path.suffix.lower()
        if not path.is_file() or suffix not in REPORT_FILE_TYPES:
            continue
        try:
            relative_to_ppt = path.relative_to(PPT_OUTPUT_DIR)
            if "template" in relative_to_ppt.parts:
                continue
            search_text = extract_report_search_text(path)
            haystack = "\n".join([path.name, str(relative_to_ppt), search_text]).lower()
            if keyword and keyword.lower() not in haystack:
                continue
            stat = path.stat()
            relative_to_project = path.relative_to(PROJECT_DIR)
            item = {
                "name": path.name,
                "type": REPORT_FILE_TYPES.get(suffix, suffix),
                "ext": suffix,
                "relative_path": relative_to_project.as_posix(),
                "url": "/" + relative_to_project.as_posix(),
                "size": stat.st_size,
                "modified": datetime.fromtimestamp(stat.st_mtime).strftime("%Y-%m-%d %H:%M:%S"),
                "mtime": stat.st_mtime,
                "snippet": build_report_snippet(search_text, keyword),
            }
            items.append(item)
        except OSError:
            continue

    items.sort(key=lambda item: item["mtime"], reverse=True)
    for item in items:
        item.pop("mtime", None)
    return {"query": keyword, "items": items[:80]}


def safe_filename(text: str) -> str:
    """生成适合 Windows 文件系统的短文件名。"""
    cleaned = re.sub(r'[\\/:*?"<>|\s]+', "_", text).strip("_")[:60]
    return cleaned or "agent_report_ppt"


def generate_ppt_from_markdown(markdown: str, title: str) -> dict[str, Any]:
    """生成独立 HTML PPT。

    优先使用项目内 consulting-html-ppt-generator 生成咨询风 deck；如果本地生成器异常，
    再回退到原 html-report-generator，保证前端按钮可用。
    """
    PPT_OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
    stamp = datetime.now().strftime("%Y%m%d_%H%M%S")
    base_name = safe_filename(title) + "_" + stamp
    markdown_path = PPT_OUTPUT_DIR / (base_name + ".md")
    html_path = PPT_OUTPUT_DIR / (base_name + ".html")
    markdown_path.write_text(markdown, encoding="utf-8")

    try:
        result = build_consulting_html_ppt(markdown, title, html_path, PROJECT_DIR)
        result["markdown_path"] = str(markdown_path.relative_to(PROJECT_DIR)).replace("\\", "/")
        result["html_url"] = "/" + str(html_path.relative_to(PROJECT_DIR)).replace("\\", "/")
        return result
    except Exception as exc:
        if not HTML_REPORT_SKILL_RUNNER.is_file():
            raise RuntimeError("咨询风 HTML PPT 生成失败，且未找到兜底执行器：" + str(exc)) from exc

    completed = subprocess.run(
        [sys.executable, str(HTML_REPORT_SKILL_RUNNER), str(markdown_path), str(html_path)],
        cwd=str(HTML_REPORT_SKILL_RUNNER.parent),
        capture_output=True,
        text=True,
        encoding="utf-8",
        timeout=60,
        check=False,
    )
    raw = (completed.stdout or "").strip()
    try:
        result = json.loads(raw)
    except json.JSONDecodeError:
        raise RuntimeError("html-report-generator 返回非 JSON 内容：" + raw[:500] + "；stderr=" + (completed.stderr or ""))

    if completed.returncode != 0 or not result.get("success"):
        raise RuntimeError(result.get("error") or "HTML PPT 生成失败")

    html = html_path.read_text(encoding="utf-8", errors="ignore")
    html = html.replace('class="slide active"', 'class="slide"', 1)
    html_path.write_text(html, encoding="utf-8")
    result["markdown_path"] = str(markdown_path.relative_to(PROJECT_DIR)).replace("\\", "/")
    result["html_url"] = "/" + str(html_path.relative_to(PROJECT_DIR)).replace("\\", "/")
    result["file_size"] = html_path.stat().st_size
    result["engine"] = "html-report-generator-fallback"
    return result


async def run_agent_once(agent_key: str, question: str) -> dict[str, Any]:
    """按前端选择调用对应业务智能体。"""
    load_dotenv(ENV_PATH, override=True)
    clear_tool_call_events()
    config = load_json_config()
    if agent_key == "user_insight_config":
        agent = build_user_insight_agent(config)
    else:
        agent_key = "market_strategy"
        agent = build_market_strategy_agent(config)

    reply = await agent.reply(UserMsg(name="业务用户", content=question))
    tool_events = get_tool_call_events()
    answer = strip_thinking(reply.get_text_content())

    if "tavily_search" in question and not any(item.get("name") == "tavily_search" for item in tool_events):
        answer += "\n\n---\n【工具审计提示】本轮问题要求使用 tavily_search，但后端未捕获到真实 tavily_search 工具调用。请检查模型是否选择了工具、TAVILY_API_KEY 是否配置，以及 AgentScope ReAct 是否进入工具调用流程。"
    if "any_search" in question and not any(item.get("name") == "any_search" for item in tool_events):
        answer += "\n\n---\n【工具审计提示】本轮问题要求使用 any_search，但后端未捕获到真实 any_search 工具调用。请检查模型是否选择了工具、ANYSEARCH_API_KEY 是否配置，以及 AgentScope ReAct 是否进入工具调用流程。"

    return {"answer": answer, "agent": agent_key, "tool_events": tool_events}


def main() -> None:
    """启动本地 Web 服务。"""
    load_dotenv(ENV_PATH, override=True)
    httpd = ThreadingHTTPServer((HOST, PORT), AgentDemoHandler)
    print("AgentScope 多智能体 Web 服务已启动: http://" + HOST + ":" + str(PORT))
    print("界面已按中汽中心AI-UI设计规范重做，并支持双 Agent 选择。")
    httpd.serve_forever()


if __name__ == "__main__":
    main()
